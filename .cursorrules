# Cursor Rules for AM&AA TMR Project

## MCP Supabase Access
- ✅ **READ/WRITE ACCESS**: You have full read and write access to Supabase project `ffgjqlmulaqtfopgwenf` via MCP
- ✅ **Available Tools**: Use MCP Supabase tools to query tables, deploy functions, check logs, etc.
- ✅ **Secrets Configured**: Core Supabase secrets are already set up
- 🔄 **Remaining Secrets**: Need to set `OPENAI_API_KEY` and `ADMIN_TOKEN` via Supabase dashboard

## Project Context
- **Supabase Project**: ffgjqlmulaqtfopgwenf
- **WordPress**: WP Engine staging environment
- **HubSpot**: Membership sync integration
- **Edge Functions**: 7 functions ready for deployment with shared utilities

## Key Reminders
- Always check Supabase state via MCP tools before making assumptions
- Use MCP tools to deploy Edge Functions when ready
- All Edge Functions have been updated with shared utilities
- Documentation reflects current MCP access status

## Current Status
- ✅ Database schema deployed
- ✅ Edge Functions updated and ready
- 🔄 Need to set remaining secrets (OpenAI key, admin token)
- 🔄 Ready for Edge Function deployment

## MANDATORY VERIFICATION PROTOCOL
- ✅ **ALWAYS use MCP tools** to verify Supabase state before making claims
- ✅ **ALWAYS search codebase** for existing implementations before assuming gaps
- ✅ **ALWAYS test functionality** with Playwright when applicable
- ✅ **NEVER assume** deployment status, database state, or functionality without verification
- ✅ **ALWAYS read** existing documentation (PRD.md, IMPLEMENTATION_PLAN.md) before updating
- ✅ **ALWAYS ask** for clarification when uncertain about current state
- ✅ **Reference** docs/runbooks/VERIFICATION_PROTOCOL.md for complete checklist
- always remember that we use supabase online, not a local. we need to make sql commands in the online terminal manually.

## MANDATORY SYNC PROTOCOL
- 🚨 **CRITICAL: NEVER test changes without confirming they are synced to staging**
- 🚨 **CRITICAL: ALWAYS ask user to confirm "Changes committed and synced to staging" before testing**
- 🚨 **CRITICAL: NEVER assume changes are live without explicit user confirmation**
- 🚨 **CRITICAL: STOP testing loops after 3-4 attempts - ask user for guidance**
- 🚨 **CRITICAL: ALWAYS clear WP Engine cache after significant code changes**
- 🚨 **CRITICAL: NEVER test without confirming changes are synced** - ask user to confirm sync before testing
